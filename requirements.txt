pydantic==2.13.4
watchdog==4.0.1
chromadb==1.3.7
openpyxl==3.1.5
werkzeug==3.1.8
flask==3.1.3
flask-wtf==1.3.0
pyyaml==6.0.3
# imported directly by the selftests (escape), not only through flask
markupsafe==3.0.3
# local speech-to-text (P14.01). the model itself is not a package: it is
# fetched once into models/stt/ at setup, pinned by revision and sha256 (see
# THIRD_PARTY_NOTICES.md), and loaded only from there - never downloaded at runtime
faster-whisper==1.2.1
ctranslate2==4.8.2
onnxruntime==1.27.0
av==18.1.0
