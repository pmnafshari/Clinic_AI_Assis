# planning and local tooling (never pushed)
.planning/
CLAUDE.md
.claude/
.env
# the patient app signs its cookies with its own key, in its own file
.env.patient
# demo-only third-party voice keys. NEVER committed - see voice_config.py
.env.voice
# copies of planning docs pulled out of .planning/ to read them - the folder
# rule above doesn't cover them once they sit at the root
/ROADMAP.md
/STATE.md
/PROJECT.md
/REQUIREMENTS.md
/MILESTONES.md
/RETROSPECTIVE.md

# python
__pycache__/
*.py[cod]
.venv/
venv/
env/
*.egg-info/
.ipynb_checkpoints/

# intermediate dataset generation output
notes_raw.jsonl

# runtime output
log.txt
sorted/

# models and data (large / private)
models/
db/
*.gguf
*.bin
*.safetensors
*.zip
notes_merged/
data/raw/

# os / editor
.DS_Store
.vscode/
.idea/
